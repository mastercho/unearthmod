// Package cdn detects which CDN, if any, fronts a target and identifies the
// CDN's published IP ranges so the engine can discard non-origin addresses.
//
// Implementation arrives in Packet 3.
package cdn
