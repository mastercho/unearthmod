// Command unearth-mcp exposes unearth's techniques over the Model Context
// Protocol.
//
// Implemented in Packet 6. This stub keeps the build honest until then.
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintln(os.Stderr, "unearth-mcp: MCP server not yet implemented -- see Packet 6")
	os.Exit(1)
}
